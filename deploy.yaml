apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-app
spec:
  template:
    spec:
      containers:
        - name: app
          image: localhost:5000/myimage
          ports:
            - containerPort: 8080
